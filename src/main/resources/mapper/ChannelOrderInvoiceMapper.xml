<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.conk.integration.command.infrastructure.mapper.ChannelOrderInvoiceMapper">

    <select id="findOrdersWithoutInvoice"
            resultType="com.conk.integration.command.application.dto.InvoiceTargetDto">
        SELECT
            co.order_id           AS orderId,
            co.receiver_name      AS receiverName,
            co.receiver_phone_no  AS receiverPhoneNo,
            co.ship_to_address1   AS shipToAddress1,
            co.ship_to_city       AS shipToCity,
            co.ship_to_state      AS shipToState,
            co.ship_to_zip_code   AS shipToZipCode
        FROM channel_order co
        WHERE co.seller_id   = #{sellerId}
          AND co.invoice_no  IS NULL
    </select>

    <!-- 주문별로 다른 invoiceNo를 한 번의 쿼리로 일괄 반영한다. -->
    <update id="bulkAssignInvoice">
        UPDATE channel_order
        SET invoice_no = CASE order_id
            <foreach collection="pairs" item="pair">
                WHEN #{pair.orderId} THEN #{pair.invoiceNo}
            </foreach>
        END
        WHERE order_id IN
        <foreach collection="pairs" item="pair" open="(" separator="," close=")">
            #{pair.orderId}
        </foreach>
    </update>

</mapper>
